{
  "name": "udacitylogger",
  "version": "2.0.0",
  "description": "it's just a logger ",
  "main": "index.js",
  "scripts": {
    "build": "tsc",
    "prestart": "npm run build",
    "start": "node dist/index.js",
    "pretest": "npm run build",
    "test": "jest --watchAll",
    "posttest": "rm -rf dist"
  },
  "keywords": [
    "logger",
    "udacity"
  ],
  "author": "tawfiek",
  "license": "MIT",
  "dependencies": {
    "winston": "^3.8.2"
  },
  "devDependencies": {
    "typescript": "^4.8.4"
  }
}
